#pragma once
// Declared under internal_headers, not public_headers: the project's own
// sources and unit tests see it through the internal tree, consumers never do.
namespace mini { inline constexpr int port_internal_value = 7; }
