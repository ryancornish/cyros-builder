name = "kernel"
description = "Public microkernel API and private kernel implementation"

dependencies = ["port"]

public_headers = [
  "kernel.hpp -> cyros/kernel.hpp",
]
public_modules = []
private_modules = []

source_roots = ["."]
generated_includes = true
